# Full requirements generated from repo scan

Flask==3.0.0
Werkzeug==3.0.1
Flask-Caching==2.1.0
Flask-Compress==1.14

# Environment
python-dotenv==1.0.0

# HTTP
requests==2.31.0

# Amazon SP-API (sp-api wrapper)
python-amazon-sp-api==0.22.0

# Data processing
pandas==2.1.4
openpyxl==3.1.2

# HTML / XML parsing
lxml==5.0.0
beautifulsoup4

# Images, barcodes, printing
Pillow==10.1.0
python-barcode==0.15.1
python-escpos==3.0
pyserial==3.5

# Bluetooth support (used by some printer backends)
pybluez

# Production server (optional)
gunicorn==21.2.0

# Notes:
# - This file includes the Python packages detected by scanning the repository.
# - Some packages are pinned to known versions; others are left unpinned where
#   the repo did not include a pinned version. You can pin them if you prefer.
# - System tools (e.g. cloudflared) are required by the app but are not Python
#   packages and must be installed on the host separately.
